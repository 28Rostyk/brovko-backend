const { addLocations } = require("./addLocation");
const { getAllLocations } = require("./getAllLocations");
const { removeLocation } = require("./removeLocation");
const { updateLocation } = require("./updateLocation");

module.exports = {
  addLocations,
  getAllLocations,
  removeLocation,
  updateLocation,
};
